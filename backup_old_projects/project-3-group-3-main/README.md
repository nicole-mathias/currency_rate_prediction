# project-3-group-3
project-3-group-3 created by GitHub Classroom

### Dhruv's branch
* `classifiers.py` : All code related to logistic regression, KNN and Naive Bayes. Predicts whether the Japanese stock market goes up or down based on the combined economic data for that day. Logistic regression performs the best!

### Nicole_branch:
#### Libraries
* Matplotlib
* Scikit learn
* Pandas


#### Run files in the following format:
* data_clean.py (this will convert numerical data into categorical data, so that classification can be performed using the target variable. This file creates train and test data and saves it in the datasets folder. Note: the train and test data will always remain the same, which was handled in our code so that all the models would be uniform).
* Run → svm.py, random_forest.py and decision_tree.py (these will use data from the datasets folder and will add confusion matrix plots to the plots folder)
Note: These models might take too long to run due to the grid search parameters and model complexities, so it is suggested to pick the best hyperparameters as given in the report and then run the code for testing purposes)

### Dhiraj Branch
- Includes the report and hypothesis #1.
#### Libraries
* scipy - `pip install scipy`
* pandas - `pip install pandas`
#### Working and output
* Run the script using `python3 hypothesis-1.py` and the output with the statistic and pvalue would be displayed on the terminal.

### Zhixuan Branch
- Includes hypothesis #2.
#### Libraries
* scipy - `pip install scipy`
* pandas - `pip install pandas`
### Run files:
* Run the script using 'python hypothsis.py' '
